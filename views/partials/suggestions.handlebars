<div class="container">
    <div class="d-flex flex-row flex-wrap">
        {{#each suggestions}}
            <div class="p-3">
                <a href="/show/{{_id}}">
                    <div class="card">
                        <div class="d-flex flex-column">
                            <div>
                                <img class="card-img-top" src="{{@root.cdnRootUrl}}/images/shows/{{_id}}/title_card/{{titleCard}}"
                                     alt="Card image cap">
                            </div>
                            <div class="my-card-title d-flex flex-column justify-content-center align-items-center">
                                <h6>{{releaseInfo}}</h6>
                                <h3>{{title}}</h3>
                            </div>
                        </div>
                    </div>
                </a>
            </div>
        {{/each}}
    </div>
</div>